#include <sys/uio.h>
#include <sys/types.h>
#include <unistd.h>
#include "bsq.h"

#define BUF_SIZE 10

void    read_entry_pipe(void)
{
    
}

void    read_entry_fd(void)
{
    open(argv)
}

void    select_entry(int argc, char **argv)
{
    if( argc > 2)
    {
        read_entry_fd(argv);
    }    
    else if
    {
        read_entry_pipe(void);
    }
}
